sequenceDiagram
    autonumber
    actor U as Utilisateur
    participant V1 as I:SignInView
    participant V2 as I:ForgotPasswordView
    participant V3 as I:OTPView
    participant V4 as I:CreateNewPasswordView
    participant C as C:AuthController
    participant Cache as E:PasswordResetCache
    participant EU as E:User
    participant M as S:EmailService

    Note over U,M: Flux complet "Mot de passe oublié" (3 étapes)

    Note right of U: voir signinDetails/forgot/email_input.txt
    ref over U,V1,V2,V3,C,Cache,EU,M: Étape 1 — Saisie de l'email + envoi OTP

    Note right of U: voir signinDetails/forgot/otp_verify.txt
    ref over U,V3,V4,C,Cache,M: Étape 2 — Saisie du code OTP (mode password_reset)

    Note right of U: voir signinDetails/forgot/new_password.txt
    ref over U,V4,V1,C,Cache,EU: Étape 3 — Nouveau mot de passe + vérification OTP backend
