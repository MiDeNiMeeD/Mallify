# Assets Folder

This folder contains all static assets for the Mallify home page.

## Structure

- `images/` - Product images, hero images, user photos
- `icons/` - SVG icons and logos

## Usage

Import images in your components:

```javascript
import heroImage from "./assets/images/hero-bg.jpg";
import logo from "./assets/icons/logo.svg";
```
